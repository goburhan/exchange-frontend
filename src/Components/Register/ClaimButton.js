import React from 'react'
import  {ClaimBtn} from "./../../StyledComponents/ClaimButton.styled"

const ClaimButton = () => {
    return (
        <ClaimBtn>
            Sing Up
        </ClaimBtn>
    );
};

export default ClaimButton